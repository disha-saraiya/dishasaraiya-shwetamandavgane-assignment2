  let state = {
    deck: rndPermute(Array.apply(0, Array(allCards.length)).map((x, y) => y).filter(wantThisCard)),
    board: Array.apply(0, Array(12)).map((x, y) => -1),
    setsfound: [],
    selected: [],
    solutions: [],
    showSolution: -1,
    stopwatch: [],
    juniorMode: juniorMode,
    showStopwatch: showStopwatch,
    errmsg: 'select 3 cards'
  };


      return <Card svgPath="svg"
                 card={crd}
                 selected={issel(crd)}
                 isInShowedSolution={highlight}
                 onClick={onclck}
    />


    allCards[a] =    shape: round,
            color: red,
            filling: solid,
            number: two

               allCards[b] =    shape: round,
            color: green,
            filling: solid,
            number: two

            let k : shape: round; 